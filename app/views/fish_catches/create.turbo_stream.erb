<%= render_update_stats_stream(@fish_catches) %>

<%= turbo_stream.prepend "catches", @fish_catch %>

<%= turbo_stream.update "new-catch" do %>
  <%= render "form", fish_catch: @new_catch %>
<% end %>

<%= render_flash_stream %>